<p id="notice"><%= notice %></p>
<div class ="container" vocab="http://schema.org/" typeof="VideoObject">
  <h1>Trailers</h1>

  <iframe width="560" height="315" src="https://www.youtube.com/embed/QwievZ1Tx-8" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe>
  <br><strong>Duração</strong>
  <p property="duration"><%= @trailers.first.duration %></p>
  <br><strong>Link</strong>
  <p property="embedUrl"><%= @trailers.first.embedUrl %></p>
  <br><strong>Data de Upload</strong>
  <p property="uploadDate"><%= @trailers.first.uploadDate %></p>
  <br><strong>Sobre</strong>
  <p property="about"><%= @trailers.first.about %></p>
  <br><strong>Filme</strong>
  <p property="movie"><%= @trailers.first.movie %></p>
  <br><strong>Classificação</strong>
  <p property="review"><%= @trailers.first.review %></p>
  <br><strong>Produtora</strong>
  <p property="publisher"><%= @trailers.first.publisher %></p><br><br>
  </div>
  <div class ="container" vocab="http://schema.org/" typeof="VideoObject">
    <iframe width="560" height="315" src="https://www.youtube.com/embed/1P9OzWX6nzE" frameborder="0" allow="autoplay; encrypted-media" allowfullscreen></iframe>
    <br><strong>Duração</strong>
    <p property="duration"><%= @trailers.last.duration %></p>
    <br><strong>Link</strong>
    <p property="embedUrl"><%= @trailers.last.embedUrl %></p>
    <br><strong>Data de Upload</strong>
    <p property="uploadDate"><%= @trailers.last.uploadDate %></p>
    <br><strong>Sobre</strong>
    <p property="about"><%= @trailers.last.about %></p>
    <br><strong>Filme</strong>
    <p property="movie"><%= @trailers.last.movie %></p>
    <br><strong>Classificação</strong>
    <p property="review"><%= @trailers.last.review %></p>
    <br><strong>Produtora</strong>
    <p property="publisher"><%= @trailers.last.publisher %></p><br><br>
  </div>
 
 
<table>
  <thead>
    <tr>
      <th>Duration</th>
      <th>Embedurl</th>
      <th>Uploaddate</th>
      <th>About</th>
      <th>Movie</th>
      <th>Review</th>
      <th>Publisher</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @trailers.each do |trailer| %>
      <tr>
        <td><%= trailer.duration %></td>
        <td><%= trailer.embedUrl %></td>
        <td><%= trailer.uploadDate %></td>
        <td><%= trailer.about %></td>
        <td><%= trailer.movie %></td>
        <td><%= trailer.review %></td>
        <td><%= trailer.publisher %></td>
        <td><%= link_to 'Show', trailer %></td>
        <td><%= link_to 'Edit', edit_trailer_path(trailer) %></td>
        <td><%= link_to 'Destroy', trailer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Trailer', new_trailer_path %>
