<%= form_with(model: director, local: true) do |form| %>
  <% if director.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(director.errors.count, "error") %> prohibited this director from being saved:</h2>

      <ul>
      <% director.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :director_name %>
  </div>

  <div class="field">
    <%= form.label :birthDate %>
    <%= form.datetime_select :birthDate, id: :director_birthDate %>
  </div>

  <div class="field">
    <%= form.label :award %>
    <%= form.text_field :award, id: :director_award %>
  </div>

  <div class="field">
    <%= form.label :gender %>
    <%= form.text_field :gender, id: :director_gender %>
  </div>

  <div class="field">
    <%= form.label :nationality %>
    <%= form.text_field :nationality, id: :director_nationality %>
  </div>

  <div class="field">
    <%= form.label :movie %>
    <%= form.text_field :movie, id: :director_movie %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
